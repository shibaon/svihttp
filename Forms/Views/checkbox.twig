<div class="form-group row">
	<div class="offset-sm-2 col-sm-5">
		<div class="checkbox">
			<input type="checkbox" id="{{ id }}" name="{{ name }}" {% if placeholder %}placeholder="{{ placeholder }}"{% endif %} {% if required %}required="1"{% endif %} {% if data %}checked{% endif %} {% if attr %}{% for k, v in attr %}{{ k }}="{{ v }}" {% endfor %}{% endif %}>
			<label for="{{ name }}">{{ label|trans }}</label>
		</div>
	</div>
</div>